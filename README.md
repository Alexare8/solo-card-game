# solo-card-game
A collection of simple command line solitaire card games.

So far that 'collection' consists of a HighCard, a dead simple test of basic functionality and *Lobo*, an actual game that's really fun! You can play lobo by cloing the repo and running `python lobo.py` in the main directory.
